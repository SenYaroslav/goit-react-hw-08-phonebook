import { Navigation } from 'components/Navigation/Navigation';

export const AppBar = () => {
  return (
    <header
      style={{
        margin: '0 auto',
      }}
    >
      <Navigation />
    </header>
  );
};
