import { Navigation } from 'components/Navigation/Navigation';

export const AppBar = () => {
  return (
    <header
      style={{
        margin: '0 auto',
        padding: '0 10px'
      }}
    >
      <Navigation />
    </header>
  );
};
